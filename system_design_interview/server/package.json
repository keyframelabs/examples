{
  "name": "@kfl-system-design/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m uvicorn app.main:app --host 127.0.0.1 --port ${PORT:-8788}"
  }
}
